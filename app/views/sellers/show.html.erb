<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @seller.name %>
</p>

<%= link_to 'Back', categories_path %>
<%= link_to 'Add item for sale', new_product_path %>

<table>
  <thead>
  <tr>
    <th>Name</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @products.each do |product| %>
      <tr>
        <td><%= link_to product.name, product %></td>
      </tr>
  <% end %>
  </tbody>
</table>