<p id="notice"><%= notice %></p>

<section class="jumbotron text-center">
  <div class="container">
    <h1 class="jumbotron-heading">Beljom</h1>
    <p class="lead text-muted">Mission Statement.</p>
    <p>
      <a href="#" class="btn btn-primary">Sign Up as a Seller</a>
  </div>
</section>

<div class="row">
  <% @categories.each do |category| %>
  <div class="col-sm-4">
    <div class="card text-center">
      <%= image_tag category.name.downcase, class: 'card-img-top' %>
      <div class="card-block">
        <h3 class="card-title"><%= category.name %></h3>
        <%= link_to 'View', category %>
      </div>

    </div>
  </div>
  <% end %>
</div>
